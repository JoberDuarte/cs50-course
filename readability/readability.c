#include <cs50.h>
#include <stdio.h>
#include <ctype.h>
#include <string.h>
#include <math.h>

int main(void)
{
    string text = get_string("TEXT: ");

}

int count_letters(string text)
{
   int letter = 0
   int len = strlen(text)

   for(i = 0; i < len; i++)
   {
    if(isalpha(text))
    {
        
    }
   }
}
